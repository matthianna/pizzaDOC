# ===========================================
# SVILUPPO LOCALE (SQLite)
# ===========================================
DATABASE_URL="file:./dev.db"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here"

# ===========================================
# PRODUZIONE VERCEL (PostgreSQL - Neon)
# ===========================================
# Usa questi valori su Vercel Dashboard → Settings → Environment Variables
# 
# DATABASE_URL con Neon PostgreSQL (usa connessione POOLED):
# DATABASE_URL="postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/dbname?sslmode=require&pgbouncer=true&connect_timeout=15"
#
# NEXTAUTH_URL (il tuo dominio Vercel):
# NEXTAUTH_URL="https://your-app.vercel.app"
#
# NEXTAUTH_SECRET (genera con: openssl rand -base64 32):
# NEXTAUTH_SECRET="generate-a-secure-secret"
#
# ⚠️ IMPORTANTE: 
# - Usa la connessione POOLED (con -pooler nell'URL) per Vercel
# - Includi sempre sslmode=require per Neon
# - Non committare mai il file .env con credenziali reali

# ===========================================
# WHATSAPP NOTIFICATIONS (WAHA)
# ===========================================
# WAHA deve essere hostato esternamente (Railway, Render, VPS)
# poiché Vercel è serverless e non supporta Docker.
#
# Setup WAHA:
# 1. Deploy WAHA su Railway: https://railway.app
# 2. Usa l'immagine Docker: devlikeapro/waha
# 3. Copia l'URL pubblico generato da Railway
#
# WAHA_URL (URL del server WAHA):
# WAHA_URL="https://waha-production-xxx.up.railway.app"
#
# WAHA_SESSION (nome della sessione WhatsApp):
# WAHA_SESSION="default"
#
# WHATSAPP_ENABLED (abilita/disabilita le notifiche):
# WHATSAPP_ENABLED="true"
#
# Per sviluppo locale (disabilita WhatsApp):
WHATSAPP_ENABLED="false"
WAHA_URL=""
WAHA_SESSION="default"

# Per Vercel (abilita WhatsApp):
# NEXT_PUBLIC_WAHA_URL="https://waha-production-xxx.up.railway.app"
# NEXT_PUBLIC_WAHA_SESSION="default"
# NEXT_PUBLIC_WHATSAPP_ENABLED="true"

# ===========================================
# CRON JOB SECURITY (Opzionale)
# ===========================================
# CRON_SECRET (per proteggere gli endpoint cron):
# CRON_SECRET="your-cron-secret-token"
#
# Se impostato, gli endpoint cron richiederanno:
# Authorization: Bearer your-cron-secret-token
